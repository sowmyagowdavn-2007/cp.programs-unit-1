#include<stdio.h>
int main()
{
   int num1,num2,i;
   printf("Enter two numbers");
   scanf("%d %d",&num1,&num2);
   if(num1>num2);
   printf("%d is greater than %d\n");
   {
       else
  if (num1<num2);
        printf("%d is less than%d\n");
   }
   {
       else(num1=num2);
        printf("%d is equal to %d\n);
   }
   return 0;
}
